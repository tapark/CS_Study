~~~sql
SELECT ANIMAL_ID
FROM ANIMAL_INS
WHERE NAME is NULL
~~~
~~~sql
SELECT ANIMAL_ID
FROM ANIMAL_INS
WHERE NAME is NOT NULL
ORDER BY ANIMAL_ID
~~~
~~~sql
SELECT  ANIMAL_TYPE, IFNULL(NAME, 'No name'), SEX_UPON_INTAKE
FROM ANIMAL_INS
ORDER BY ANIMAL_ID
~~~